# This file previously contained real API tokens and has been scrubbed.
#
# ⚠ The UniFi and Redpoint tokens that used to live here should be
#    considered exposed and must be rotated before first live run.
#    See the warning at the top of DEPLOY.md.
#
# Start from .env.shadow.example instead:
#     cp .env.shadow.example .env
#     chmod 600 .env
#     # then fill in real values in .env (never commit .env)
#
# .env is ignored by git (see .gitignore). Keep it that way.
